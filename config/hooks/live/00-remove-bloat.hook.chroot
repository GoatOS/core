#!/bin/sh
echo "REMOVENDO BLOATWARE..."

# Lista de pacotes para remover (apenas se existirem)
apt-get purge -y --auto-remove \
    libreoffice* \
    gimp* \
    transmission-gtk \
    simple-scan \
    atril \
    eom \
    2>/dev/null || true

apt-get clean
